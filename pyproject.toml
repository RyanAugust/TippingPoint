[project]
name = "dxpoint"
description = "Differential media response curve modeling and strategic inflection point optimization"
dynamic = ["version"]
readme = "README.md"
keywords = [
    "dxpoint",
    "marketing",
    "adstock",
    "hill-function",
    "saturation",
    "derivative",
    "mmm",
    "roas"
]

classifiers = [
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "Development Status :: 3 - Alpha",
]
authors = [
    { name = "Ryan Duecker", email = "ryanduecker@gmail.com" },
]
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "tinygrad",
    "matplotlib",
    "streamlit",
    "plotly",
    "scipy"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/dxpoint/__init__.py"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "ruff",
    "pre-commit",
    "hatch",
]
lint = ["ruff","pylint"]
test = ["pytest"]
docs = [
    "sphinx>=6.0",
]

[project.scripts]
dxpoint = "dxpoint.cli:main"
dxpt = "dxpoint.cli:main"
tipp = "dxpoint.cli:main"

[tool.hatch.metadata]
vcs = true

[tool.hatch.build.targets.sdist]
exclude = [
    "/.github",
    "/tests",
    "/research",
]

[tool.hatch.build.targets.wheel]
packages = ["src/dxpoint"]
exclude = [
    "/.github",
    "/tests",
    "/.vscode",
    "/research",
]

pre-install-commands = ["pip install --upgrade pip",]

[tool.hatch.envs.default.scripts]
# Run tests with pytest
test = "pytest {args}"
# Run tests with coverage
cov = "pytest --cov={package} --no-cov-on-fail {args}"
# Run ruff linter/formatter
lint = "ruff check {package} {args}"
format = "ruff format {package} {args}"
# Build the package
build = "hatch build"
# Clean build artifacts
clean = "hatch clean"

[tool.ruff]
preview = true
target-version = "py311"
line-length = 250
indent-width = 2
exclude = [
  ".git/",
  "docs/",
]

[tool.ruff.lint]
select = [
  "F",  # Pyflakes
  "W6",
  "E71",
  "E72",
  "E112",   # no-indented-block
  "E113",   # unexpected-indentation
  # "E124",
  "E203",   # whitespace-before-punctuation
  "E272",   # multiple-spaces-before-keyword
  "E275",   # missing-whitespace-after-keyword
  "E303",   # too-many-blank-lines
  "E304",   # blank-line-after-decorator
  "E501",   # line-too-long
  # "E502",
  "E702",   # multiple-statements-on-one-line-semicolon
  "E703",   # useless-semicolon
  "E731",   # lambda-assignment
  "W191",   # tab-indentation
  "W291",   # trailing-whitespace
  "W293",   # blank-line-with-whitespace
  "UP039",  # unnecessary-class-parentheses
  "C416",   # unnecessary-comprehension
  "RET506", # superfluous-else-raise
  "RET507", # superfluous-else-continue
  "A",      # builtin-variable-shadowing, builtin-argument-shadowing, builtin-attribute-shadowing
  "FURB110",# if-exp-instead-of-or-operator
  "RUF018", # assignment-in-assert
]

[tool.ruff.format]
exclude = ["*"]

[tool.coverage.run]
branch = true
source = ["dxpoint"]

